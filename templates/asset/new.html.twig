{% extends 'base.html.twig' %}

{% block title %}New Asset{% endblock %}

{% block body %}
    <h1 class="text-center">Create new Asset</h1>
    <div class="container">
    {{ include('asset/_form.html.twig') }}
    </div>
    <a href="{{ path('asset_index') }}">back to list</a>
{% endblock %}
