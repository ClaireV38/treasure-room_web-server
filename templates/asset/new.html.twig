{% extends 'base.html.twig' %}

{% block title %}Nouveau trésor{% endblock %}

{% block body %}
    <h1 class="text-center">Ajouter un nouveau trésor</h1>
    <div class="container">
        {{ include('asset/_form.html.twig') }}
    </div>
    <div class="d-flex flex-row justify-content-center mt-2">
        <a class="m-auto btn btn-primary" href="{{ path('adventurer_index') }}">Retour</a>
    </div>
{% endblock %}
